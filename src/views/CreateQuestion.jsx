import CreateQuestionCss from '../assets/css/createQuestion.module.css';

// import { Link } from "react-router-dom";

const CreateQuestion = () => {
  return (
    <section className={`${CreateQuestionCss}`}>
      질문만들기
    </section>
  );
};

export default CreateQuestion;
